<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Arch-notes on László Szurok</title>
        <link>https://laszloszurok.github.io/arch-notes/</link>
        <description>Recent content in Arch-notes on László Szurok</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <copyright>&lt;a href=&#34;https://creativecommons.org/licenses/by-nc/4.0/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CC BY-NC 4.0&lt;/a&gt;</copyright>
        <lastBuildDate>Sat, 04 Jul 2020 17:09:50 +0200</lastBuildDate>
        <atom:link href="https://laszloszurok.github.io/arch-notes/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>Pre-Installation</title>
            <link>https://laszloszurok.github.io/arch-notes/pre-installation/</link>
            <pubDate>Sat, 04 Jul 2020 17:09:50 +0200</pubDate>
            
            <guid>https://laszloszurok.github.io/arch-notes/pre-installation/</guid>
            <description>Connecting to the internet I have a wireless card in my laptop, but it is not working in a live enviroment due to driver problems. I am going to fix it after the installation. For now I&amp;rsquo;m going to use my phone to share its wifi connection with the machine through usb. For this to work we need to start a program called dhcpcd.
❯ dhcpcd Now you can plug in your phone and share its internet connection with the laptop.</description>
            <content type="html"><![CDATA[<h2 id="connecting-to-the-internet">Connecting to the internet</h2>
<p>I have a wireless card in my laptop, but it is not working in a live enviroment due to driver problems. I am going to fix it after the installation. For now I&rsquo;m going to use my phone to share its wifi connection with the machine through usb. For this to work we need to start a program called <a href="https://wiki.archlinux.org/index.php/Dhcpcd">dhcpcd</a>.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ dhcpcd 
</code></pre><p>Now you can plug in your phone and share its internet connection with the laptop. Test the connection with the following command.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ ping archlinux.org
</code></pre><h2 id="time-and-date">Time and date</h2>
<p>Update the system clock with <a href="https://wiki.archlinux.org/index.php/System_time#System_clock">timedatectl</a>.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ timedatectl set-ntp true
</code></pre><h2 id="partitioning-the-disk">Partitioning the disk</h2>
<p>I am using <a href="https://wiki.archlinux.org/index.php/Fdisk">fdisk</a> to create my partitions. Use <a href="https://wiki.archlinux.org/index.php/Device_file">lsblk</a> to get the appropriate device names. (eg. /dev/sda)</p>
<pre><code class="language-terminal" data-lang="terminal">❯ fdisk /path/to/device
</code></pre><p>For UEFI:</p>
<ol>
<li><strong>g</strong> - for a new GUID Partition Table</li>
<li><strong>n</strong> - for the efi partition, partition number: default, first sector: default, last sector: +512M</li>
<li><strong>n</strong> - for a swap partition (optional), partition number: default, first sector: default, last sector: +8G (in my case)</li>
<li><strong>n</strong> - for the root partition, partition number: default, first sector: default, last sector: default</li>
<li><strong>w</strong> - to write the changes on the disk</li>
</ol>
<p>For MBR:</p>
<ol>
<li><strong>o</strong> - for a new DOS Partition Table</li>
<li><strong>n</strong> - for a swap partition (optional), partition number: default, first sector: default, last sector: +8G (in my case)</li>
<li><strong>n</strong> - for the root partition, partition number: default, first sector: default, last sector: default</li>
<li><strong>w</strong> - to write the changes on the disk</li>
</ol>
<h2 id="creating-filesystems">Creating filesystems</h2>
<p>Use the device name in the paths (eg. mkfs.ext4 /dev/sda3)</p>
<p>If you created an efi partition:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mkfs.fat -F32 /path/to/efi/partition
</code></pre><p>If you created a swap partition:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mkswap /path/to/swap/partition
</code></pre><p>For the root partition:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mkfs.ext4 /path/to/root/partiton
</code></pre><h2 id="mounting-the-partitons">Mounting the partitons</h2>
<p>Now we are going to attach the created partitions to the existing filesystem.</p>
<p>First the root partition:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mount /path/to/root/partition /mnt
</code></pre><p>If you created an efi partiton:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mkdir /mnt/boot
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ mkdir /mnt/boot/EFI
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ mount /path/to/efi/partition /mnt/boot/EFI
</code></pre><p>If you created a swap partiton:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ swapon /path/to/swap/partition
</code></pre>]]></content>
        </item>
        
        <item>
            <title>Installation</title>
            <link>https://laszloszurok.github.io/arch-notes/installation/</link>
            <pubDate>Fri, 03 Jul 2020 10:12:02 +0200</pubDate>
            
            <guid>https://laszloszurok.github.io/arch-notes/installation/</guid>
            <description>More info on the wiki.
Setting up the mirrorlist For faster download speeds choose the appropriate mirrors based on your country. I&amp;rsquo;m going to use vim to edit the mirrorlist file.
❯ vim /etc/pacman.d/mirrorlist Search for your country and move the lines to the top of the file (uncomment them). Save the file and exit the texteditor.
Installing the base system Install the necessary packages and a texteditor of your preference.</description>
            <content type="html"><![CDATA[<p>More info on the <a href="https://wiki.archlinux.org/index.php/Installation_guide">wiki</a>.</p>
<h2 id="setting-up-the-mirrorlist">Setting up the mirrorlist</h2>
<p>For faster download speeds choose the appropriate mirrors based on your country. I&rsquo;m going to use vim to edit the <a href="https://wiki.archlinux.org/index.php/Mirrors">mirrorlist</a> file.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ vim /etc/pacman.d/mirrorlist
</code></pre><p>Search for your country and move the lines to the top of the file (uncomment them). Save the file and exit the texteditor.</p>
<h2 id="installing-the-base-system">Installing the base system</h2>
<p>Install the necessary packages and a texteditor of your preference.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ pacstrap /mnt base linux linux-firmware vim
</code></pre>]]></content>
        </item>
        
        <item>
            <title>Post-Installation</title>
            <link>https://laszloszurok.github.io/arch-notes/post-installation/</link>
            <pubDate>Thu, 02 Jul 2020 10:14:55 +0200</pubDate>
            
            <guid>https://laszloszurok.github.io/arch-notes/post-installation/</guid>
            <description>Generating the filesystem table Creating the fstab file:
❯ genfstab -U /mnt &amp;gt;&amp;gt; /mnt/etc/fstab Chroot into the installed system With the chroot command you leave the live enviroment and enter the newly installed system.
❯ arch-chroot /mnt Timezone and locale Choose your timezone:
❯ ln -sf /usr/share/zoneinfo/Europe/Budapest /etc/localtime Run hwclock to generate /etc/adjtime:
❯ hwclock --systohc Uncomment the needed locales in /etc/locale.gen (in my case en_US.UTF-8):
❯ vim /etc/locale.gen Generate the locales:</description>
            <content type="html"><![CDATA[<h2 id="generating-the-filesystem-table">Generating the filesystem table</h2>
<p>Creating the <a href="https://wiki.archlinux.org/index.php/Fstab">fstab</a> file:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ genfstab -U /mnt &gt;&gt; /mnt/etc/fstab
</code></pre><h2 id="chroot-into-the-installed-system">Chroot into the installed system</h2>
<p>With the <a href="https://wiki.archlinux.org/index.php/Chroot">chroot</a> command you leave the live enviroment and enter the newly installed system.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ arch-chroot /mnt
</code></pre><h2 id="timezone-and-locale">Timezone and locale</h2>
<p>Choose your <a href="https://wiki.archlinux.org/index.php/System_time#Time_zone">timezone</a>:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ ln -sf /usr/share/zoneinfo/Europe/Budapest /etc/localtime
</code></pre><p>Run <a href="https://wiki.archlinux.org/index.php/System_time#Hardware_clock">hwclock</a> to generate /etc/adjtime:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ hwclock --systohc
</code></pre><p>Uncomment the needed <a href="https://wiki.archlinux.org/index.php/Locale">locales</a> in /etc/locale.gen (in my case en_US.UTF-8):</p>
<pre><code class="language-terminal" data-lang="terminal">❯ vim /etc/locale.gen
</code></pre><p>Generate the locales:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ locale-gen
</code></pre><p>Set the LANG variable in locale.conf:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ echo LANG=en_US.UTF-8 &gt;&gt; /etc/locale.conf
</code></pre><h2 id="network-configuration">Network configuration</h2>
<p>Choose a <a href="https://wiki.archlinux.org/index.php/Network_configuration#Set_the_hostname">hostname</a> (in my case arch):</p>
<pre><code class="language-terminal" data-lang="terminal">❯ echo arch &gt;&gt; /etc/hostname
</code></pre><p>To edit the <a href="https://wiki.archlinux.org/index.php/Network_configuration#Local_hostname_resolution">hosts</a> file:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ vim /etc/hosts
</code></pre><p>Enter the following information (replace arch with your hostname):</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">127.0.0.1   localhost
::1         localhost
127.0.1.1   arch.localdomain    arch
</code></pre></div><h2 id="installing-important-packages">Installing important packages</h2>
<pre><code class="language-terminal" data-lang="terminal">❯ pacman -S grub efibootmgr networkmanager network-manager-applet wireless_tools wpa_supplicant dhcpcd os-prober mtools dosfstools base-devel linux-headers
</code></pre><ul>
<li><strong>grub</strong>: the bootloader we are going to use</li>
<li><strong>efibootmgr</strong>: only needed if you installed the system in UEFI mode</li>
<li><strong>networkmanager, network-manager-applet, wireless-tools, wpa_supplicant, dhcpcd</strong>: networking tools</li>
<li><strong>os-prober</strong>: needed if you want to dualboot this system with an other OS later</li>
<li><strong>mtools, dosfstools</strong>: filesystem tools</li>
<li><strong>base-devel, linux-headers</strong>: package groups which include basic libraries that you will probably need later</li>
</ul>
<h2 id="setting-up-grub-grand-unified-bootloader">Setting up GRUB (Grand Unified Bootloader)</h2>
<p>Installing <a href="https://wiki.archlinux.org/index.php/GRUB">GRUB</a>:</p>
<p>UEFI:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ grub-install --target=x86_64-efi --efi-directory=/boot/EFI --bootloader-id=GRUB
</code></pre><p>MBR (use the device name the system is installed on):</p>
<pre><code class="language-terminal" data-lang="terminal">❯ grub-install --target=i386-pc /dev/sda
</code></pre><p>Creating the configuration file:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ grub-mkconfig -o /boot/grub/grub.cfg
</code></pre><h2 id="user-account-settings">User account settings</h2>
<p>More info on the <a href="https://wiki.archlinux.org/index.php/Users_and_groups">wiki</a>.</p>
<p>Set the root password:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ passwd
</code></pre><p>Create a user (replace username with your own username):</p>
<pre><code class="language-terminal" data-lang="terminal">❯ useradd -m username
</code></pre><p>Set a password for the new user:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ passwd username
</code></pre><p>Add the user to some important groups (no spaces after the commas):</p>
<pre><code class="language-terminal" data-lang="terminal">❯ usermod -aG wheel,audio,video,optical,storage username
</code></pre><p>To let the new user gain root privileges when needed, we have to edit a file called sudoers. Open the file with the following command:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ visudo
</code></pre><p>Uncomment this line:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-bash" data-lang="bash">%wheel ALL<span style="color:#f92672">=(</span>ALL<span style="color:#f92672">)</span> ALL
</code></pre></div><p>Save and exit the editor.</p>
<h2 id="exiting-and-rebooting">Exiting and rebooting</h2>
<p>To exit the installation back to the live enviroment:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ exit
</code></pre><p>To unmount all partitions:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ umount -a
</code></pre><p>Ignore the warning messages about the busy partitions.</p>
<p>Now reboot the system and log in with the user account you created previously.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ reboot
</code></pre><h2 id="network-services-and-wifi">Network services and WiFi</h2>
<p>Starting and eanbling <a href="https://wiki.archlinux.org/index.php/Network_configuration">network</a> services.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ systemctl enable --now NetworkManager
</code></pre><p>As I mentioned my wireless card is not working yet, I&rsquo;m going to fix it now.</p>
<p>Installing pre-requirements:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S dkms git
</code></pre><p>Installing the wireless driver:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git clone https://github.com/lwfinger/rtw88.git
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ cd rtw88
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ make
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ sudo make install
</code></pre><p>Installing the driver as a <a href="https://wiki.archlinux.org/index.php/Kernel_module">kernel module</a> with <a href="https://wiki.archlinux.org/index.php/Dynamic_Kernel_Module_Support">dkms</a>, so it will be rebuilt automatically at kernel updates:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo dkms add ./rtw88
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ sudo dkms install rtlwifi-new/0.6
</code></pre><h2 id="accessing-the-aur">Accessing the AUR</h2>
<p>The <a href="https://wiki.archlinux.org/index.php/Arch_User_Repository">Arch User Repository</a> - as the name suggests - is a repository which contains software made by the community. You can not access this repository directly with pacman. You have to install
a software called an AUR helper to be able to install packages from this repo. My favourite one is <a href="https://github.com/Jguer/yay">yay</a>. You can install it with the following commands:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git clone https://aur.archlinux.org/yay.git
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ cd yay
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ makepkg -si
</code></pre><h2 id="setting-up-a-graphical-environment-xorg">Setting up a graphical environment (xorg)</h2>
<p>First of all we are going to install graphical drivers.</p>
<p>For Intel cards:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S xf86-video-intel
</code></pre><p>For AMD cards:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S xf86-video-amdgpu
</code></pre><p>Then we are going to install <a href="https://wiki.archlinux.org/index.php/Xorg">xorg</a>.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S xorg
</code></pre><p>We need to install a package called <a href="https://wiki.archlinux.org/index.php/Xinit">xorg-xinit</a> as well:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S xorg-xinit
</code></pre><p>I am going to install my build of <a href="https://wiki.archlinux.org/index.php/Dwm">DWM</a>, which needs the following two fonts to work properly:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S ttf-font-awesome ttf-dejavu
</code></pre><p>I&rsquo;m cloning all my <a href="https://suckless.org/">suckless builds</a>, config files and scripts from my github. I&rsquo;m using a git bare repository to manage my config- and other files in my home folder.</p>
<p>If you want my configs just clone the repo from the below link as you normally would and then place its content to your home folder.</p>
<p>My scripts may or may not work on your machine, as some of them are specific to the hardware in my laptop.</p>
<p>So in my case:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git clone --separate-git-dir=$HOME/.myconf https://github.com/laszloszurok/suckless-arch.git $HOME/myconf-tmp
</code></pre><p>This clones my files to a temporary directory and creates a bare repo in my home folder. We need the temporary directory because if the home folder is not empty we can&rsquo;t clone directly into it with this command. Now we have to move the files from myconf-tmp to the home folder:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ mv ~/myconf-tmp/* ~/myconf-tmp/.[!.]* ~/
</code></pre><p>This moves every file - including hidden ones - to the home dir. We don&rsquo;t need the tmp folder anymore and there is a .git file that we can remove too:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ rm -r ~/myconf-tmp/ ~/.git
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ alias config='/usr/bin/git --git-dir=$HOME/.myconf/ --work-tree=$HOME'
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ config config status.showUntrackedFiles no
</code></pre><p>The .myconf folder will be the bare repo. This is a great way of managing config files, because you can basicly forget about it after the setup. You don&rsquo;t have to deal with the .myconf folder, just leave it there in your home directory. With the alias, you can use commands like this to manage your files:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ config status
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ config add .bashrc
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ config commit -m &quot;updated .bashrc&quot;
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ config push
</code></pre><p>Write this alias in your .bashrc or .zshrc to make it permanent. Obviously, if you want to manage your files like this, you have to set up your own git repo, because you can&rsquo;t push to mine. You can do this with these commands:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git init --bare $HOME/.myconf
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ alias config='/usr/bin/git --git-dir=$HOME/.myconf/ --work-tree=$HOME'
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ config config status.showUntrackedFiles no
</code></pre><p>Then you have to add your own remote to the repository.</p>
<p>Now go into the suckless-builds/dwm directory and execute the following command:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo make install
</code></pre><p>Execute the above command for all of my suckless builds to install them. Now that DWM is installed we need a way to launch it. For this, you have to copy a configuration file to your home folder and edit it, as follows:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ cp /etc/X11/xinit/xinitrc /home/youruser/.xinitrc
</code></pre><p>Replace &lsquo;youruser&rsquo; with your username. Open the copied file and delete the last five lines containing twm, xclock, etc. Replace it with with the following:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-bash" data-lang="bash">exec dwm
</code></pre></div><p>Save the file and exit the editor. Now you should be able to launch dwm with this command:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ startx
</code></pre><p>Make sure you have a terminal emulator installed before running startx. If you installed all of my suckless builds you have <a href="https://wiki.archlinux.org/index.php/St">st</a>.</p>
<p>If you don&rsquo;t want to launch the X server manually with the startx command every time you start up your computer, you have to install a display manager. I am going to install <a href="https://wiki.archlinux.org/index.php/LightDM">lightdm</a>.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S lightdm
</code></pre><p>We also need a greeter (a graphical login screen). My favourite one is called <a href="https://github.com/linuxmint/slick-greeter">lightdm-slick-greeter</a>. It&rsquo;s in the AUR, so you have to install it with an AUR helper. I am using yay.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ yay -S lightdm-slick-greeter
</code></pre><p>We have to make some configurations to make lightdm work. Open the file located at /etc/lightdm/lightdm.conf with a texteditor and uncomment the following line under the [LightDM] section:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">[LightDM]
...
sessions-directory=/usr/share/lightdm/sessions:/usr/share/xsessions:/usr/share/wayland-sessions
...
</code></pre></div><p>Now under the [Seat:*] section, set the default greeter to lightdm-slick-greeter and also set the user-session to dwm as you can see here:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">[Seat:*]
...
greeter-session=lightdm-slick-greeter
...
user-session=dwm
...
</code></pre></div><p>For the user-session to work we have to create a file called dwm.desktop. Place this file to the following location: /usr/share/xsessions/ . Open the file with a texteditor and write
the following configuration into it:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">[Desktop Entry]
Encoding=UTF-8
Name=dwm
Comment=Dynamic Window Manager
Exec=/usr/local/bin/dwm
Type=Application
</code></pre></div><p>Now lightdm is configured, but we have to enable it with <a href="https://wiki.archlinux.org/index.php/Systemd">systemctl</a>. This way lightdm will automatically launch when you boot up your computer.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ systemctl enable lightdm
</code></pre><p>So we have a display manager with a greeter, but the greeter is just a black screen and a login form by default. If you want to set a wallpaper, you can set it through the
lightdm-slick-greeter configuration file, but there is a nice graphical tool in the AUR called lightdm-settings which lets you manage the greeter&rsquo;s settings in an easy way.
Install it with the following command:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ yay -S lightdm-settings
</code></pre><p>After you reboot, you should see the lightdm-slick-greeter login screen and DWM should automatically start after you log in.</p>
<p>Now I&rsquo;m going to apply a nice dark theme for the system. My favourite dark theme is ArcDark, whit the Arc icon-theme. Install these with the following command:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S arc-gtk-theme arc-icon-theme
</code></pre><p>The tools I&rsquo;m using for managing themes are lxappearance for GTK and qt5ct for QT. Install them:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S lxappearance qt5ct
</code></pre><p>To apply the themes globally, add these <a href="https://wiki.archlinux.org/index.php/Environment_variables">environmental variables</a> to /etc/environment:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">GTK_THEME=Arc-Dark
QT_QPA_PLATFORMTHEME=qt5ct
</code></pre></div><h2 id="configuring-the-touchpad">Configuring the touchpad</h2>
<p>Now I&rsquo;m going to configure the touchpad of my laptop, because tap-to-click and natural scrolling are turned off by default. Create a file called 30-touchpad.conf and place it to /etc/X11/xorg.conf.d/ . Write these settings into it:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">Section &#34;InputClass&#34;
    Identifier &#34;touchpad&#34;
    Driver &#34;libinput&#34;
    MatchIsTouchpad &#34;on&#34;
    Option &#34;Tapping&#34; &#34;on&#34;
    Option &#34;NaturalScrolling&#34; &#34;true&#34;
EndSection
</code></pre></div><p>Restart the X server for the changes to take effect.</p>
<h2 id="enabling-sound">Enabling sound</h2>
<p>I&rsquo;m using the <a href="https://wiki.archlinux.org/index.php/Advanced_Linux_Sound_Architecture">ALSA</a> sound system on my machine. Install alsa-utils with the following command. This will provide a program called alsa-mixer which you can use to control sound.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S alsa-utils
</code></pre><p>I have media control buttons on my laptop (they can control for eg. switching to next/prev. song on Spotify). For these to work I&rsquo;m going to install playerctl.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S playerctl
</code></pre><p>I have alsamixer and playerctl commands binded to the volume level controlling and media controlling keys in my DWM config, thats how I make these keys functional.</p>
]]></content>
        </item>
        
        <item>
            <title>Setting up my most used programs</title>
            <link>https://laszloszurok.github.io/arch-notes/other-software/</link>
            <pubDate>Sat, 09 May 2020 18:32:02 +0200</pubDate>
            
            <guid>https://laszloszurok.github.io/arch-notes/other-software/</guid>
            <description>The Z program The z program will make your life easier on the command line. Installation:
❯ git clone https://github.com/rupa/z ~/z ❯ mkdir ~/.cache/z ❯ echo &amp;quot;. ~/z/z.sh&amp;quot; &amp;gt;&amp;gt; ~/.zshrc ❯ exec $SHELL Use it like this:
❯ cd grandparent-folder/parent-folder/child-folder ❯ cd different/path/to/another/folder ❯ z child-folder The last command will take you back directly inside the directory called child-folder. The z program automatically updates itself as you use it, so it will always know the path of the folders you visited.</description>
            <content type="html"><![CDATA[<h2 id="the-z-program">The Z program</h2>
<p>The <a href="https://github.com/rupa/z">z program</a> will make your life easier on the command line. Installation:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git clone https://github.com/rupa/z ~/z
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ mkdir ~/.cache/z
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ echo &quot;. ~/z/z.sh&quot; &gt;&gt; ~/.zshrc
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ exec $SHELL
</code></pre><p>Use it like this:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ cd grandparent-folder/parent-folder/child-folder
❯ cd different/path/to/another/folder
❯ z child-folder
</code></pre><p>The last command will take you back directly inside the directory called child-folder. The z program automatically updates itself as you use it, so it will always know the path of the folders you visited. Makes it much faster to move around in the filesystem.</p>
<h2 id="spotify-settings">Spotify settings</h2>
<p>I have a workspace deticated to Spotify in my DWM build. By default Spotify is not following the rules specified in DWM&rsquo;s config.h file, so the window won&rsquo;t be moved to the right workspace when you launch the program. Here&rsquo;s the fix for this problem:</p>
<p>Install a little program called spotifywm. This will help us to give Spotify a classname when it starts up, so DWM will be able to apply the rules to the Spotify window.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ git clone https://github.com/dasJ/spotifywm.git
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ cd spotifywm
</code></pre><pre><code class="language-terminal" data-lang="terminal">❯ make
</code></pre><p>The make command will build a binary file called spotifywm.so.</p>
<p>Now create a file called spotify under this location: /usr/local/bin/ with the following content:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-text" data-lang="text">LD_PRELOAD=/usr/lib/libcurl.so.4:/home/&lt;USERNAME&gt;/spotifywm/spotifywm.so /usr/bin/spotify
</code></pre></div><p>Replace &lt;USERNAME&gt; with your username, then save the file and make it executable. Now if you launch Spotify it should open on the workspace it&rsquo;s assigned to.</p>
<h2 id="changing-the-default-shell-to-zsh">Changing the default shell to zsh</h2>
<p>Install zsh:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S zsh
</code></pre><p>Change the shell for the current user:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ chsh -s /usr/bin/zsh
</code></pre><h2 id="pywal-color-genearation">Pywal color genearation</h2>
<p>I like to add some extra prettiness to my window manager. <a href="https://github.com/dylanaraps/pywal">Pywal</a> can generate colors based on your wallpaper and you can use them in your configuration files to have a unified colorscheme across your system. It will also set your terminal colors.</p>
<p>Install it from the AUR:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ yay -S python-pywal
</code></pre><p>You also need a utility to set a chosen image as your wallpaper. I&rsquo;m using <a href="https://wiki.archlinux.org/index.php/Feh">feh</a>.</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo pacman -S feh
</code></pre><p>Run the following command to set your wallpaper and generate your colors:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ wal -i /path/to/an/image
</code></pre><p>Your terminal colors will be set automatically, but not permanently. To apply the colors for every new terminal window, you have to add the following line to your .bashrc or .zshrc file:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-bash" data-lang="bash"><span style="color:#f92672">(</span>cat ~/.cache/wal/sequences &amp;<span style="color:#f92672">)</span>
</code></pre></div><p>Now configure DWM to use the generated colors. Open the config.h file and delete or comment out the following lines:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-c" data-lang="c"><span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> col_gray1[]       <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;#222222&#34;</span>;
<span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> col_gray2[]       <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;#444444&#34;</span>;
<span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> col_gray3[]       <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;#bbbbbb&#34;</span>;
<span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> col_gray4[]       <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;#eeeeee&#34;</span>;
<span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> col_cyan[]        <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;#005577&#34;</span>;
<span style="color:#66d9ef">static</span> <span style="color:#66d9ef">const</span> <span style="color:#66d9ef">char</span> <span style="color:#f92672">*</span>colors[][<span style="color:#ae81ff">3</span>]      <span style="color:#f92672">=</span> {
	<span style="color:#75715e">/*               fg         bg         border   */</span>
	[SchemeNorm] <span style="color:#f92672">=</span> { col_gray3, col_gray1, col_gray2 },
	[SchemeSel]  <span style="color:#f92672">=</span> { col_gray4, col_cyan,  col_cyan  },
};
</code></pre></div><p>Replace them with this single line:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-c" data-lang="c"><span style="color:#75715e">#include</span> <span style="color:#75715e">&#34;/home/&lt;USER&gt;/.cache/wal/colors-wal-dwm.h&#34;</span><span style="color:#75715e">
</span></code></pre></div><p>Replace &lt;USER&gt; with your username.</p>
<p>You have to reinstall DWM to apply the changes:</p>
<pre><code class="language-terminal" data-lang="terminal">❯ sudo make clean install
</code></pre><p>Now restart DWM to see the new colors.</p>
<h2 id="dunst">Dunst</h2>
<p><a href="https://wiki.archlinux.org/index.php/Dunst">Dunst</a> is a little program for showing notifications on the desktop.
My configuration file for it is on my <a href="https://github.com/laszloszurok/suckless-arch/tree/master/.config/dunst">github page</a>. There is a little script as well, which will apply pywal generated colors for the notifications. You have to autostart this script for the colors to be applied. My DWM build has the autostart patch and executes this script when you log in.</p>
<h2 id="vifm">Vifm</h2>
<p><a href="https://wiki.archlinux.org/index.php/Vifm">Vifm</a> is a terminal file manager. It uses vim like keybindings.
You can set <a href="https://vifm.info/colorschemes.shtml">color schemes</a> for it, my favourite is the palenight theme. To apply a theme, download and place it
inside .config/vifm/colors/ . Then open .config/vifm/vifmrc and add this line:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-vim" data-lang="vim"><span style="color:#a6e22e">colorscheme</span> <span style="color:#a6e22e">palenight</span><span style="color:#960050;background-color:#1e0010">
</span></code></pre></div><p>By default vifm won&rsquo;t show image previews. To make that work, you need to add the following configuration to vifmrc:</p>
<div class="highlight"><pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-vim" data-lang="vim"><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">pdf</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">pdfpreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">epub</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">epubpreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">avi</span>,*.<span style="color:#a6e22e">mp4</span>,*.<span style="color:#a6e22e">wmv</span>,*.<span style="color:#a6e22e">dat</span>,*.<span style="color:#ae81ff">3</span>gp,*.<span style="color:#a6e22e">ogv</span>,*.<span style="color:#a6e22e">mkv</span>,*.<span style="color:#a6e22e">mpg</span>,*.<span style="color:#a6e22e">mpeg</span>,*.<span style="color:#a6e22e">vob</span>,<span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \*.<span style="color:#a6e22e">fl</span>[<span style="color:#a6e22e">icv</span>],*.<span style="color:#a6e22e">m2v</span>,*.<span style="color:#a6e22e">mov</span>,*.<span style="color:#a6e22e">webm</span>,*.<span style="color:#a6e22e">ts</span>,*.<span style="color:#a6e22e">mts</span>,*.<span style="color:#a6e22e">m4v</span>,*.<span style="color:#a6e22e">r</span>[<span style="color:#a6e22e">am</span>],*.<span style="color:#a6e22e">qt</span>,*.<span style="color:#a6e22e">divx</span>,<span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">videopreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">bmp</span>,*.<span style="color:#a6e22e">jpg</span>,*.<span style="color:#a6e22e">jpeg</span>,*.<span style="color:#a6e22e">png</span>,*.<span style="color:#a6e22e">xpm</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">draw</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">gif</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">gifpreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> *.<span style="color:#a6e22e">ico</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">magickpreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>        <span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> &lt;<span style="color:#a6e22e">audio</span>/*&gt;<span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">audiopreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>        <span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span><span style="color:#a6e22e">fileviewer</span> &lt;<span style="color:#a6e22e">font</span>/*&gt;<span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">fontpreview</span> %<span style="color:#a6e22e">px</span> %<span style="color:#a6e22e">py</span> %<span style="color:#a6e22e">pw</span> %<span style="color:#a6e22e">ph</span> %<span style="color:#a6e22e">c</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ %<span style="color:#a6e22e">pc</span><span style="color:#960050;background-color:#1e0010">
</span><span style="color:#960050;background-color:#1e0010"></span>    \ <span style="color:#a6e22e">vifmimg</span> <span style="color:#a6e22e">clear</span><span style="color:#960050;background-color:#1e0010">
</span></code></pre></div><p>Then clone this repo: <a href="https://github.com/cirala/vifmimg">https://github.com/cirala/vifmimg</a> and place the vifmimg and vifmrun scripts in a folder that is included in your $PATH variable.
From now on, you have to launch vifm with the vifmrun script to have image-, pdf-, video thumbnail-, etc. previews. For faster access: alias vf=vifmrun.</p>
]]></content>
        </item>
        
        <item>
            <title>Screenshots</title>
            <link>https://laszloszurok.github.io/arch-notes/screenshots/</link>
            <pubDate>Tue, 14 Apr 2020 20:48:38 +0200</pubDate>
            
            <guid>https://laszloszurok.github.io/arch-notes/screenshots/</guid>
            <description>Vifm filemanager  Spotify  Random windows to look cool  Empty desktop  One for r/unixporn </description>
            <content type="html"><![CDATA[<h2 id="vifm-filemanager">Vifm filemanager</h2>
<p>
    <img src="/img/screenshots/vifm.png"  alt="Where"  class="center"  style="border-radius: 4px;"  />


<br>

    <img src="/img/screenshots/2020-07-14-200430_1920x1080_scrot.png"  alt="vifm"  class="center"  style="border-radius: 4px;"  />

</p>
<h2 id="spotify">Spotify</h2>
<p>
    <img src="/img/screenshots/2020-07-14-200641_1920x1080_scrot.png"  alt="is"  class="center"  style="border-radius: 4px;"  />


<br>

    <img src="/img/screenshots/2020-07-14-200702_1920x1080_scrot.png"  alt="my"  class="center"  style="border-radius: 4px;"  />


<br>

    <img src="/img/screenshots/spotify-2.png"  alt="mind"  class="center"  style="border-radius: 4px;"  />

</p>
<h2 id="random-windows-to-look-cool">Random windows to look cool</h2>
<p>
    <img src="/img/screenshots/2020-07-14-204050_1920x1080_scrot.png"  alt="?"  class="center"  style="border-radius: 4px;"  />


<br>

    <img src="/img/screenshots/random-2.png"  alt="random"  class="center"  style="border-radius: 4px;"  />

</p>
<h2 id="empty-desktop">Empty desktop</h2>
<p>
    <img src="/img/screenshots/2020-07-14-204120_1920x1080_scrot.png"  alt="empty"  class="center"  style="border-radius: 4px;"  />


<br>

    <img src="/img/screenshots/empty-2.png"  alt="empty"  class="center"  style="border-radius: 4px;"  />

</p>
<h2 id="one-for-runixporn">One for r/unixporn</h2>

    <img src="/img/screenshots/unixporn.png"  alt="unixporn"  class="center"  style="border-radius: 4px;"  />


]]></content>
        </item>
        
    </channel>
</rss>
